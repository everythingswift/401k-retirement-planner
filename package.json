{
  "name": "retirement-planner",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @retirement/web dev",
    "build": "pnpm --filter @retirement/core build && pnpm --filter @retirement/web build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
